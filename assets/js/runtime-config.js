window.DLF_RUNTIME = {
  // Configure seu número com DDI + DDD + número (somente dígitos).
  whatsappNumber: "5534996829438",

  // Ative quando tiver backend online.
  useBackend: true,

  // Exemplo: https://dlf-api.onrender.com
  apiBaseUrl: "http://localhost:10000",

  // Opcional: sobrescrever endpoints caso precise.
  // endpoints: {
  //   products: "/api/products",
  //   product: "/api/products/:id",
  //   orders: "/api/orders",
  //   shipping: "/api/shipping"
  // }
};
